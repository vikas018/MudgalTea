import type { Testimonial } from './types';

// TODO(vikas): supply three REAL customer quotes — name, location, rating (1-5), text.
// The previous entries used stock photos of strangers and named a product ("Assam Gold")
// the shop doesn't sell, so they were removed. The Testimonials section renders nothing
// until this array has entries. Do not use stock faces for real named people.
export const testimonials: Testimonial[] = [];
